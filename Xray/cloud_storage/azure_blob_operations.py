import os
import sys
from Xray.exception  import XrayException


class azureBolbOperation:

    def sync_folder_to_blob()->None:
        try:
            pass
        except Exception as e:
            raise XrayException(e,sys)
        
    def sync_blob_to_folder()->None:
        try:
            pass
        except Exception as e:
            raise XrayException(e,sys)

